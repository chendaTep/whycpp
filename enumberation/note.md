p# Enum in C++

**What is enum?**  
- `enum` (enumeration) is a way to **give names to a set of related integer values**.  
- Makes code **readable, organized, and less error-prone**.

---

**Syntax:**
```cpp
enum Name { VALUE1, VALUE2, VALUE3 };
 

this is example 


enum Color { RED, GREEN, BLUE };
Color myColor = GREEN;  // instead of 1




# Enum Quick Note

**Enum = Named set of related numbers**  
- Makes code **readable, safe, and organized**  
- Example: `enum Color { RED, GREEN, BLUE };`  
- Use for **limited fixed choices** like states, options, or codes


so enum are often use in the switch statement 
