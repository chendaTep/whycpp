#include <chrono>
#include <iostream>
#include <string>
#include <thread>

class Person {
protected:
  std ::string name;
  int age;
  int id;

public:
  Person(std ::string n, int a, int ID) : name(n), age(a), id(ID) {}

  void displayInfo() { int winth std ::cout << }
};
